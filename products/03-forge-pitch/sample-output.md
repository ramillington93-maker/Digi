# Sample output

This is the real, unedited output of:

```
python cli.py --job samples/job_post_1.txt --profile samples/sample_profile.txt
```

No API key was set for this run — this is template mode, the free
happy path every buyer gets by default.

Input job post: `samples/job_post_1.txt` (a Django/API bug-fix job with
a `$400-$800` budget). Input profile: `samples/sample_profile.txt`
(Jamie Rivera, Python/Django backend freelancer, $55/hr).

---

# Proposal for: Backend developer needed to fix API bugs and add a new endpoint

Detected niche: **web development**
Detected tone: **neutral**
Budget hint found in post: **$400-$800**

## Subject line options
1. Re: Backend developer needed to fix API bugs... -- a quick fit check
2. Jamie's note on your web development post
3. Proposal: Backend developer needed to fix API... (with samples)

## Proposal body

You wrote: "We run a small SaaS product built on Python (Django) with a PostgreSQL database." -- that's squarely Jamie's lane. I work in python day to day, and this reads like a project I could start on this week, not next month.

5 years building and maintaining backend APIs for early-stage startups. Recent work covers python, api, django, database, which lines up with what you listed in the post (web development).

How I'd approach it:
1. Confirm scope and any existing code, repo, or hosting access.
2. Build in short milestones so you can review progress, not just a final drop.
3. Test against the cases you care about before calling it done.

Reply with a yes and I'll send a short kickoff message with next steps -- no call required unless you want one.

-- Jamie Rivera
jamierivera.dev
$55/hr

## Price anchors
- **Low anchor: $460** -- Fixed scope, single deliverable, no revisions beyond one round. Good if budget is the objection.
- **Mid anchor (recommended): $600** -- Full scope as posted, two revision rounds, async check-ins. Default offer -- lead with this one.
- **Premium anchor: $780** -- Full scope plus a fast turnaround or an extra deliverable (e.g. source files, a short handoff call).

## Objection-handling P.S.
P.S. -- If $400-$800 is tight, tell me the number and I'll scope the low-anchor version above so it fits. No hard feelings either way.

---
_Generated in template mode -- no API key used._

---

Note how the price anchors ($460 / $600 / $780) are computed from the
client's own stated `$400-$800` range, not a fixed markup — a job post
with no budget mentioned instead anchors off the freelancer's own rate.
Run the other two sample job posts (`job_post_2.txt`, a content-writing
gig, and `job_post_3.txt`, an urgent data-cleanup gig) to see the niche
detection, tone detection, and price anchors all shift accordingly.
