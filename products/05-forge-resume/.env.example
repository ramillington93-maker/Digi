# Both are optional. Without either, ForgeResume still scores your resume
# against the job description offline and gives you missing keywords.
# Add a key here only to upgrade the STAR bullet rewrite and summary
# generator from the rule-based template to an LLM-written version.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
