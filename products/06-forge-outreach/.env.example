# ForgeOutreach — optional AI polish
# Not required. The 5-email sequence is built with template logic and
# works fully with no keys set. Set one of these ONLY if you want the
# optional "Polish tone with AI" checkbox to do a real rewrite pass.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
